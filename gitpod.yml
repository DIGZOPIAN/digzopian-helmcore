image:
  file: .gitpod.Dockerfile

tasks:
  - init: pip install -r requirements.txt || true
    command: python3 main.py

vscode:
  extensions:
    - ms-python.python

ports:
  - port: 8000
    onOpen: open-preview
